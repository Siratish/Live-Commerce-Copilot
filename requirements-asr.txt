# Optional dependencies for real Whisper inference.
# The default demo path does not require these packages.
openai-whisper==20240930
transformers>=4.38.0,<5
accelerate>=0.26.0
torch>=2.1.0
numpy>=1.23.0
